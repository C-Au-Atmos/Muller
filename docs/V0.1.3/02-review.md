# Muller V0.1.3 评审记录

## 文档信息

| 字段 | 值 |
|---|---|
| 目标版本 | `V0.1.3` |
| 所属分支 | `req/0.1.3` |
| 文档状态 | `Reviewed` |
| 评审负责人 | `Chen XingYe` |
| 最后更新 | `2026-08-24` |

## 评审规则

- 每项评审对应 [`01-original-input.md`](01-original-input.md) 中的稳定条目 ID。
- 结论只使用 `Accepted`、`Deferred`、`Rejected`、`Needs information`。
- 前三项生命周期需求继承 Stage 7.11 来源文档的产品决策和 `P0/P1/P0` 分级；
  目标版本 `V0.1.3` 来自迁移指令。历史 ID 仅用于追踪，不代替当前规范 ID。
- Stage 7.11 源文件最初为“提案”。接受依据 `SRC-711-ACCEPT` 是 2026-08-10
  同一 Codex 任务对话中的后续用户指令：“ok，现在根据req-0.1.3分支下的7.11文档
  修改代码，3个功能”。该指令只覆盖前三项生命周期需求。对话未随仓库归档，本次
  整理从原任务历史核对；代码是否已经存在不作为接受依据。
- `BUG-0.1.3-001` 的接受依据是 2026-08-24 用户上报及用户明确要求把修复写入
  V0.1.3 req 分支；评审人为 `Chen XingYe`，技术实现负责人为 `Chen TianHao`。
- 本文中的技术栈、改动模块、风险和测试策略属于评审结论，不回写原始输入。

## 评审索引

| 条目 ID | 历史 ID | 结论 | 优先级 | 目标版本 | 技术栈摘要 | 主要影响范围 | 负责人 |
|---|---|---|---|---|---|---|---|
| `REQ-0.1.3-001` | `MUL-LIFE-001` | `Accepted` | `P0` | `V0.1.3` | React、TypeScript、Rust、Tauri | 设置、偏好、窗口生命周期 | `TBD` |
| `REQ-0.1.3-002` | `MUL-LIFE-002` | `Accepted` | `P1` | `V0.1.3` | React、TypeScript、Rust、Tauri、Windows Registry、NSIS | 设置、原生命令、自启动、安装器 | `TBD` |
| `REQ-0.1.3-003` | `MUL-LIFE-003` | `Accepted` | `P0` | `V0.1.3` | Rust、Tauri、Windows 同步原语 | 应用启动、单实例 IPC、窗口恢复 | `TBD` |
| `BUG-0.1.3-001` | `None` | `Accepted` | `P1` | `V0.1.3` | React、TypeScript、WebView2、Playwright | 浏览搜索、IME 事件、快捷键、查询调度 | `Chen TianHao` |

<a id="req-0-1-3-001"></a>

## `REQ-0.1.3-001` - 关闭主窗口行为可选

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-3-001`](01-original-input.md#req-0-1-3-001)
- 历史 ID：`MUL-LIFE-001`
- 评审日期：`2026-08-13`
- 参与人：`需求提交人（姓名未记录）；文档整理人；技术负责人 TBD`
- 当前结论：`Accepted`
- 优先级：`P0`
- 目标版本或重审条件：`V0.1.3`

### 价值与范围判断

- 用户价值：把关闭窗口从固定隐藏改为可预测、可持久化的用户选择，同时保留托盘
  驻留工作流。
- 问题影响：所有使用主窗口关闭入口的用户都会遇到；当前无法通过设置改变行为。
- 当前版本范围：Settings 的“启动与关闭”分组、主窗口等价关闭请求、托盘退出、
  默认值与恢复默认、持久化和中英文错误状态。
- 非目标：不新增退出确认；不改变最小化按钮、子窗口、系统窗口、失焦行为和现有
  全局快捷键；不重新定义后台扫描与文件操作合同。
- 依赖与阻塞：需要前端设置状态与 Rust 原生窗口事件使用同一已持久化值；写入
  失败时必须保留之前生效的选择。

### 技术评审

- 建议技术栈：React 19、TypeScript、Tauri 2 command/window event、Rust、JSON
  配置持久化和现有 i18n。
- 预计改动模块：`src/features/settings/`、`src/features/lifecycle/`、
  `src/preferences/`、`src/i18n/`、`src-tauri/src/lifecycle.rs`、
  `src-tauri/src/lib.rs`、前端单元测试和 `e2e/`。
- 数据或接口变更：增加 `hide | quit` 关闭行为；提供读取和写入原生状态的 Tauri
  command；旧数据、未知枚举或损坏设置回退为 `hide`。
- 性能与资源影响：设置读取与写入为低频小文件 I/O；关闭事件只读取内存状态，
  不应引入可感知延迟。
- 安全与文件操作风险：配置写入失败、前后端状态漂移或错误拦截系统退出可能导致
  用户误以为应用已退出，或阻断注销、关机和更新退出。
- 测试策略：偏好解析和持久化单元测试；Tauri command 与关闭事件集成测试；
  `X`、`Alt+F4`、任务栏关闭、托盘退出及恢复默认的 Windows 人工测试。
- 替代方案：仅使用 WebView `localStorage` 无法保证原生关闭事件读取到可靠的最新
  值；每次关闭弹确认框不符合“不新增确认流程”的范围，均不采用。

### 版本决策

- 是否延期：`No`
- 决策理由：这是主窗口生命周期的基础控制，范围明确，可复用现有设置、托盘和
  Tauri 窗口事件；来源已给出完整默认值、失败回退和验收行为。
- 验收条件：
  - [ ] “隐藏到系统托盘”为默认值，切换后下一次关闭立即生效，重启后保持。
  - [ ] `X`、`Alt+F4` 和任务栏关闭在两种选项下行为一致。
  - [ ] 隐藏时 PID、托盘、全局快捷键、工作区和允许的后台任务保持；恢复同一窗口。
  - [ ] 退出时走正常退出流程并释放托盘和全局快捷键；再次启动为冷启动。
  - [ ] 托盘“Quit Muller”始终真正退出，Windows 注销、关机和更新退出不被拦截。
  - [ ] 损坏设置和写入失败不会阻止启动或错误切换当前生效行为。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-08-10 | `Accepted` | 需求提交人（姓名未记录） | `SRC-711-ACCEPT` 明确要求按 7.11 文档实现三项功能；继承来源文档 P0 分级。 |
| 2026-08-13 | `Accepted` | 文档整理人 | 按当前模板迁移为 `REQ-0.1.3-001`，结论不变。 |

<a id="req-0-1-3-002"></a>

## `REQ-0.1.3-002` - 当前用户登录时自动启动

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-3-002`](01-original-input.md#req-0-1-3-002)
- 历史 ID：`MUL-LIFE-002`
- 评审日期：`2026-08-13`
- 参与人：`需求提交人（姓名未记录）；文档整理人；技术负责人 TBD`
- 当前结论：`Accepted`
- 优先级：`P1`
- 目标版本或重审条件：`V0.1.3`

### 价值与范围判断

- 用户价值：由用户自主决定登录后是否让 Muller 静默驻留，并确保设置页展示的是
  Windows 实际状态。
- 问题影响：希望长期使用托盘和快捷唤出的用户当前必须每次手动启动；应用也缺少
  对注册失败、升级路径变化和卸载残留的处理。
- 当前版本范围：当前用户自启动开关、隐藏且不抢焦点的登录启动、手动显示意图
  优先、实际注册状态同步、错误回滚、路径刷新和卸载清理。
- 非目标：不实现所有用户或管理员级自启动、计划任务、登录前服务、延迟启动、
  看门狗、退出后重启或安装器 UI 重设计。
- 依赖与阻塞：依赖 Windows 当前用户注册表权限、可靠区分 `--autostart` 与手动
  启动、单实例能力以及 NSIS 卸载钩子；注册表实机验证必须在可恢复的测试账号进行。

### 技术评审

- 建议技术栈：React/TypeScript 设置 UI、Tauri 2 command、Rust `winreg`、
  Windows `HKCU` Run/StartupApproved 状态和 NSIS 卸载钩子。
- 预计改动模块：`src/features/settings/`、`src/features/lifecycle/`、
  `src/preferences/`、`src/i18n/`、`src-tauri/src/lifecycle.rs`、
  `src-tauri/src/lib.rs`、`src-tauri/tauri.conf.json`、`src-tauri/installer.nsh`。
- 数据或接口变更：增加自启动状态读取/设置 command；注册值使用当前可执行文件的
  带引号路径及 `--autostart`；Windows 实际注册状态为权威，前端值仅作缓存。
- 性能与资源影响：只在进入设置、切换开关和启动刷新时访问少量注册表数据；登录
  启动不得创建可见窗口或抢占焦点。
- 安全与文件操作风险：路径未正确引用、注册表权限失败、外部禁用状态被误覆盖、
  升级后旧路径残留或卸载未清理会产生失效启动项；不得写入其他用户范围。
- 测试策略：命令生成、状态调和和错误结构单元测试；设置 UI mock 集成测试；非
  管理员 Windows 用户上的启用、停用、重新登录、并发手动启动、升级和卸载测试。
- 替代方案：Tauri autostart 插件可减少注册代码，但仍需证明能读取 Windows 实际
  禁用状态、刷新安装路径并执行卸载清理；计划任务和 HKLM 超出当前用户范围。

### 版本决策

- 是否延期：`No`
- 决策理由：自启动是托盘驻留体验的一部分，来源已明确默认关闭、实际状态权威、
  登录隐藏和单实例竞态合同，可与另外两项在同一生命周期层实现和验证。
- 验收条件：
  - [ ] 新安装和恢复默认时关闭，当前用户不存在 Muller 自启动项。
  - [ ] 开启后重新登录只启动一个隐藏主实例和一个托盘图标，不抢前台焦点。
  - [ ] 关闭后重新登录不启动 Muller，切换开关不额外启动或结束当前进程。
  - [ ] 外部删除或禁用注册项、注册或注销失败时，UI 回到实际状态并显示本地化错误。
  - [ ] 登录启动与手动启动竞态只保留一个实例，且最终窗口可见。
  - [ ] 升级后指向当前路径且无重复项，卸载清理由 Muller 创建的注册项。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-08-10 | `Accepted` | 需求提交人（姓名未记录） | `SRC-711-ACCEPT` 明确要求按 7.11 文档实现三项功能；继承来源文档 P1 分级。 |
| 2026-08-13 | `Accepted` | 文档整理人 | 按当前模板迁移为 `REQ-0.1.3-002`，结论不变。 |

<a id="req-0-1-3-003"></a>

## `REQ-0.1.3-003` - 单实例与桌面图标唤出

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-3-003`](01-original-input.md#req-0-1-3-003)
- 历史 ID：`MUL-LIFE-003`
- 评审日期：`2026-08-13`
- 参与人：`需求提交人（姓名未记录）；文档整理人；技术负责人 TBD`
- 当前结论：`Accepted`
- 优先级：`P0`
- 目标版本或重审条件：`V0.1.3`

### 价值与范围判断

- 用户价值：让桌面图标等入口成为可靠的“启动或唤出”动作，避免重复进程、托盘
  图标和可写工作区，同时保留已有界面与任务状态。
- 问题影响：所有重复启动、快速双击以及登录启动和手动启动竞态都可能受影响；
  当前没有单实例锁或二次启动通知合同。
- 当前版本范围：每个 Windows 用户交互会话的单主实例、启动阶段串行化、手动与
  登录启动意图区分、既有窗口恢复和激活、异常退出后的正常冷启动。
- 非目标：不支持多个独立主实例、多窗口工作区、文件参数或深链接转发、强制结束
  无响应实例按钮；不重新设计桌面快捷方式或开始菜单入口。
- 依赖与阻塞：依赖可靠的实例身份和二次启动 IPC；窗口尚未就绪时必须保存一次
  显示意图；Windows 前台激活限制需要可见性与任务栏提示的降级行为。

### 技术评审

- 建议技术栈：Rust、Tauri 2 `tauri-plugin-single-instance`、Windows 当前会话命名
  同步原语、Tauri WebviewWindow API 和启动参数意图。
- 预计改动模块：`src-tauri/src/lib.rs`、`src-tauri/src/startup_gate.rs`、
  `src-tauri/src/lifecycle.rs`、`src-tauri/Cargo.toml`、`Cargo.lock`，以及启动和窗口
  状态的 Rust/Windows 集成测试。
- 数据或接口变更：二次手动启动向主实例发送“显示”意图；登录启动携带后台意图；
  主窗口未就绪时在主实例内暂存并一次性消费显示请求。
- 性能与资源影响：健康主实例接收请求到窗口可见的耗时应在参考设备上建立基线；
  同步门只覆盖启动初始化，不得长期阻塞正常运行。
- 安全与文件操作风险：启动竞态可能绕过单实例约束；永久锁、死锁或误把登录启动
  当成手动启动会阻止启动或错误抢焦点；不得用模糊进程名作为实例身份。
- 测试策略：命名启动门并发单元测试；冷启动、启动中竞态、隐藏、最小化、后台
  可见、最大化、连续启动和异常退出后的 Windows 多进程测试；工作区状态回归测试。
- 替代方案：仅按进程名查找存在误匹配和竞态；仅使用命名互斥量缺少可靠唤出 IPC；
  仅注册单实例插件而不覆盖主实例初始化窗口可能丢失快速二次启动意图。

### 版本决策

- 是否延期：`No`
- 决策理由：重复实例会破坏托盘和工作区的一致性，也是登录自启动正确性的前置
  能力；来源给出了完整状态矩阵和失败边界。
- 验收条件：
  - [ ] 无进程时手动启动得到一个主实例、主窗口和托盘图标，窗口可见并激活。
  - [ ] 隐藏、最小化、后台可见、最大化时再次启动均复用同一 PID 并正确恢复状态。
  - [ ] 唤出不刷新 WebView、不重置路由或工作区、不取消后台任务。
  - [ ] 窗口未就绪时的二次启动和连续快速启动 10 次最终仍只有一个主实例。
  - [ ] 异常结束旧实例后可正常冷启动；既有实例无响应时不创建第二个可写实例。
  - [ ] 记录健康实例的唤出耗时基线，并验证 Windows 限制抢焦点时的降级提示。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-08-10 | `Accepted` | 需求提交人（姓名未记录） | `SRC-711-ACCEPT` 明确要求按 7.11 文档实现三项功能；继承来源文档 P0 分级。 |
| 2026-08-13 | `Accepted` | 文档整理人 | 按当前模板迁移为 `REQ-0.1.3-003`，结论不变。 |

<a id="bug-0-1-3-001"></a>

## `BUG-0.1.3-001` - 浏览模式搜索栏兼容微软拼音和微信输入法

### 评审输入

- 原始输入：[`01-original-input.md#bug-0-1-3-001`](01-original-input.md#bug-0-1-3-001)
- 历史 ID：`None`
- 来源：`用户上报`
- 评审日期：`2026-08-24`
- 参与人：`用户（姓名未记录）；评审人 Chen XingYe；实现负责人 Chen TianHao`
- 当前结论：`Accepted`
- 优先级：`P1`
- 目标版本或重审条件：`V0.1.3`

### 价值与范围判断

- 用户价值：让 Windows 中文用户能够在浏览模式中使用系统常见输入法稳定完成
  候选确认、取消和中文搜索，不因 Muller 抢占按键或焦点而中断输入。
- 问题影响：浏览搜索是核心文件定位流程；微软拼音属于常见系统输入法，候选确认
  和取消分别与搜索提交、清空快捷键冲突，缺少合理的日常输入规避方式。
- 当前版本范围：浏览顶部常驻搜索框的组合输入生命周期、最终查询提交、当前目录/
  递归/全盘模式、单栏/双栏分发，以及组合期间的全局快捷键隔离；共享
  `DirectorySearchBar` 使用同一事件语义，避免比较与重复结果搜索保留同类缺陷。
- 非目标：不实现拼音或五笔模糊匹配，不自绘输入法候选窗，不按输入法品牌建立
  白名单，不改变 Rust 搜索算法、索引、过滤和分页语义，也不扩大为所有可编辑控件
  的输入框重构；不对未纳入安装版验收的所有第三方输入法、Windows 或 WebView2
  历史版本作无条件兼容承诺。
- 依赖与阻塞：需要按 WebView2/Chromium 的 `compositionstart`、`input/change`、
  `keydown`、`compositionend` 实际顺序管理本地预编辑值和已提交查询，并保证失焦、
  切换搜索模式或卸载组件时不会留下错误的组合状态。

### 技术评审

- 建议技术栈：React 19、TypeScript、标准 DOM Composition Events、Tauri 2
  WebView2、Vitest 和 Playwright/Edge E2E。
- 预计改动模块：`src/App.tsx`、`src/commands/appCommands.ts`、
  `src/features/explorer/BrowseWorkspace.tsx`、
  `src/features/explorer/DirectorySearchBar.tsx`、
  `src/features/explorer/useDirectoryPane.ts`、相关前端测试和 `e2e/stage710.spec.ts`。
- 事件与数据合同：组合期间保留输入法预编辑显示，但不把预编辑文本传给目录搜索，
  不启动、取消或关闭搜索 task/session；`compositionend` 后把最终文字同步为正式
  查询，并按既有 120 ms 调度对每个目标 pane 最多执行一次。实现必须防止组合
  结束前后的事件顺序导致最终查询丢失或重复。
- 键盘合同：`event.isComposing` 为真或 WebView2 兼容键值为 `229` 时，Muller 不得
  消费 `Enter`、`Escape` 或其他候选操作键，不得 `preventDefault`、提交、清空、
  失焦、选择首个结果或触发全局命令。组合结束后的普通按键保持原有语义。
- 数据或接口变更：不新增 Tauri command、配置、持久化或迁移；最终 Unicode 查询
  继续使用现有 Rust 目录搜索接口，后端不是本缺陷的修改目标。
- 性能与资源影响：组合期间停止无效的预编辑查询和 task/session 取消重建，应减少
  后台工作；最终查询仍使用现有 120 ms 防抖，不新增可感知输入延迟。
- 安全与文件操作风险：错误的全局 `Escape` 路由可能取消正在运行的扫描；错误的
  组合状态复位可能吞掉普通 `Enter`/`Escape`、遗失最终文字或使搜索永远不更新；
  受控输入的上层状态回写还可能重置候选窗、预编辑文本或光标位置。
- 测试策略：为组合状态判定和最终值提交增加单元/组件测试；Edge E2E 按真实事件
  顺序验证调用次数、焦点、模式和扫描并行场景；Windows Tauri 安装版分别使用微软
  拼音和微信输入法验证候选窗。浏览器事件模拟不能替代实机验收。
- 替代方案：单纯延长防抖仍会查询预编辑文本；按输入法名称分支无法覆盖版本和
  第三方差异；只拦截 `Enter` 会遗漏 `Escape` 和全局快捷键，均不采用。

### 版本决策

- 是否延期：`No`
- 决策理由：缺陷影响 Windows 中文用户的核心浏览搜索，已在当前事件链稳定复现，
  根因和前端修改边界明确；用户已明确要求纳入 V0.1.3，由 `Chen TianHao` 实现。
  当前未发现数据损坏、安全问题或应用完全不可用，因此定为 `P1` 而非 `P0`。
- 验收条件：
  - [ ] 组合预编辑持续超过 120 ms 时，不发出预编辑文本查询，不取消或关闭现有
    搜索 task/session，当前结果和焦点保持稳定。
  - [ ] 组合态 `Enter` 确认候选时不提交搜索、不选择或聚焦结果；最终中文确定后，
    输入框保持焦点，并只用最终文字对每个目标 pane 执行一次查询。
  - [ ] 组合态 `Escape` 只交给输入法处理，不清空已提交搜索词、不失焦、不关闭
    搜索，也不取消正在运行的扫描。
  - [ ] `event.isComposing` 与兼容键值 `229` 两条路径都受保护；当前目录、递归、
    全盘以及单栏、双栏搜索都只接收最终文字。
  - [ ] 普通英文/数字、粘贴、`Ctrl+F` 和非组合态 `Enter`/`Escape` 的既有行为
    保持，比较与重复结果搜索的共享输入组件不保留同类冲突。
  - [ ] 自动化测试构造完整组合事件顺序并断言查询调用次数与焦点；现有 `.fill()`
    用例只能作为普通输入回归，不能作为 IME 验收证据。
  - [ ] Windows Tauri 安装版分别使用微软拼音和微信输入法完成 `Enter` 确认、
    `Escape` 取消、中文最终搜索和扫描并行测试，并记录 Windows、WebView2、输入法
    与产物版本。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-08-24 | `Accepted` | `Chen XingYe` | 来源为用户上报；同意作为 `P1` 缺陷纳入 V0.1.3，由 `Chen TianHao` 负责技术实现。 |
