# Muller V0.1.5 评审记录

## 文档信息

| 字段 | 值 |
|---|---|
| 目标版本 | `V0.1.5` |
| 所属分支 | `req/0.1.5` |
| 文档状态 | `Complete` |
| 评审负责人 | `Codex` |
| 最后更新 | `2026-09-13` |

## 评审索引

| 条目 ID | 结论 | 优先级 | 目标版本 | 技术栈摘要 | 主要影响范围 | 负责人 |
|---|---|---|---|---|---|---|
| `REQ-0.1.5-001` | `Accepted` | `P1` | `V0.1.5` | Git、Markdown、远端分支管理 | 版本分支、历史记录、文档索引 | Codex |
| `REQ-0.1.5-002` | `Accepted` | `P1` | `V0.1.5` | Rust/Tauri、React/TypeScript、Canvas、文件系统元数据 | 空间扫描、treemap 布局、交互、音效 | Codex |
| `REQ-0.1.5-003` | `Accepted` | `P1` | `V0.1.5` | Canvas 线性布局、Rust 搜索审计、Windows MFT/USN 架构 | 空间视图性能、搜索性能分级、后续索引服务 | Codex |
| `REQ-0.1.5-004` | `Accepted` | `P1` | `V0.1.5` | Win32 MFT/USN、按需提权进程、本机命名管道 | 原生文件名索引、GUI 搜索、Windows EXE | Codex |
| `BUG-0.1.5-001` | `Accepted` | `P1` | `V0.1.5` | Rust 增量统计、React 批次快照、Canvas squarified treemap | 空间视图扫描、布局、动画和 Platinum 视觉 | Codex |
| `REQ-0.1.5-005` | `Accepted` | `P2` | `0.1.5-beta.3` | React/TypeScript、Canvas 空间导航、GSAP、现有文件操作 | 空间交互、外观设置、Target Cursor、黑透配色、右键菜单 | Codex |

<a id="req-0-1-5-001"></a>

## `REQ-0.1.5-001` - 建立 V0.1.5 开发分支并归档旧版本分支

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-5-001`](01-original-input.md#req-0-1-5-001)
- 评审日期：`2026-09-12`
- 当前结论：`Accepted`
- 优先级：`P1`
- 目标：`V0.1.5 开始前完成`

### 价值与范围判断

- 用户价值：建立明确的 V0.1.5 需求和开发入口，降低旧版本清理后的查找成本。
- 当前版本范围：`建立 req/0.1.5、feat/0.1.5；固化六个旧阶段尖端；更新活动版本入口和历史索引；删除本地及远端旧分支。`
- 非目标：不删除提交对象、版本发布标签或 V0.1.3/V0.1.4 文档；不创建尚未有实现和验证依据的 `release/0.1.5`。
- 依赖与阻塞：依赖 `master` 已包含所有旧阶段尖端，且远端引用已同步。

### 技术评审

- 建议技术栈：Git 非强制分支/标签操作、Markdown 文档索引。
- 安全与文件操作风险：删除前检查尖端已被 `master` 包含；保留版本标签与归档标签。
- 测试策略：检查工作区、祖先关系、标签指向、远端引用、文档链接和 Git 空白。
- 替代方案：只依赖 reflog 不适合远端追溯；采用注释归档标签加 Markdown 索引。

### 版本决策

- 是否延期：`No`
- 验收条件：
  - [x] `req/0.1.5` 和 `feat/0.1.5` 从 `master` 基线建立。
  - [x] 六个旧阶段尖端有 `archive/*` 注释标签和 SHA 索引。
  - [x] 本地和远端旧阶段分支清理完成，旧提交仍可从标签、`master` 和文档追溯。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-09-12 | `Accepted` | Codex | 旧阶段均已合入 master；以归档标签和文档索引保留阶段级追溯能力。 |

<a id="req-0-1-5-002"></a>

## `REQ-0.1.5-002` - Space Sniffer 风格的高性能空间方块视图

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-5-002`](01-original-input.md#req-0-1-5-002)
- Platinum 实机截图：[muller-platinum-live.png](design/muller-platinum-live.png)
- 设计附件：[Space Sniffer 空间视图线框](design/space-sniffer-wireframe.svg)
- 交互流程图：[Muller V0.1.5 Space Sniffer Interaction Flow](https://www.figma.com/board/SxWYnaNaS31cnTHtlOLvGY?utm_source=other&utm_content=edit_in_figjam&oai_id=&request_id=948d8780-8eaa-4566-a601-4285c406cf8a)
- 评审日期：`2026-09-12`
- 参与人：`Codex`
- 当前结论：`Accepted`
- 优先级：`P1`
- 目标版本或重审条件：`V0.1.5；补齐规模、性能目标和视图入口后进入 Accepted`

### 价值与范围判断

- 用户价值：用空间比例直接发现大目录、大文件和空间异常，减少逐层打开目录的时间。
- 问题影响：递归扫描和大规模矩形绘制同时涉及磁盘 I/O、线程、内存和交互帧率，若没有渐进式架构容易卡住主界面。
- 当前版本范围：`空间视图入口、首层及递归子层 treemap、渐进统计、方块钻取、鼠标/键盘/选框选择、细分隔线与正交拐角、音效事件、取消和错误状态。`
- 非目标：不读取文件内容；不跟随符号链接；不实现跨磁盘比较、自动清理或 SpaceSniffer 品牌视觉复制。
- 依赖与阻塞：测试版先限定为单栏当前目录、本地可读路径和逻辑字节数；典型规模、性能预算和双栏支持在基准阶段补充，不阻塞原型实现。

### 技术评审

- 扫描架构：Rust 后台会话一次递归枚举元数据，使用有界并行和批量事件推送 `discovered/partial/completed/failed`，支持取消、背压和扫描代次。
- 数据语义：默认统计 logical bytes；协议中的字节数使用字符串或 BigInt 安全表示；符号链接和 junction 默认不跟随，权限失败保留错误节点。
- 缓存策略：以规范化路径、文件 ID（可用时）、大小、修改时间和目录版本校验；缓存只用于增量和钻取首屏，不能替代最终扫描。
- 布局与绘制：使用确定性的 squarified 或 slice-and-dice treemap；布局在 worker 或 Rust 侧计算；Canvas/WebGL 批量绘制方块、1px 光线和 90°正交边界，DOM overlay 提供键盘和 ARIA。
- 交互：单击选择，双击或 Enter 打开目录，Esc/面包屑返回；拖拽选框命中可见矩形；小块聚合或只显示悬浮详情。
- 视觉基线：依据 0.1.4 实机 Platinum 截图，使用 `#09090b`、`#121215`、`#151518`、`#27272a`、`#2f2f35` 和 `#ffffff`；方块只做灰阶层次，细线和选框使用白色，状态色仅用于扫描/错误小提示。
- 音效：复用 `useInterfaceAudio` 的 tick/action/navigate/success/warning 和限流；新增 tile 事件必须去重，扫描异常不得误报成功音效。
- 性能风险：网络盘、机械盘、权限错误、目录持续变化和百万级文件会产生长尾；必须展示扫描状态，限制并发和内存，并允许立即停止。
- 测试策略：Rust 统计/取消/错误测试，面积守恒和稳定布局测试，Canvas/WebGL 选框命中测试，音效节流单测，Windows 基准和 Edge E2E。
- 替代方案：纯前端逐项 IPC 或纯 DOM 方块都会放大 I/O 和布局成本，因此先验证 Rust 扫描 + 批量事件 + Canvas/WebGL。

### 版本决策

- 是否延期：`No`
- 决策理由：先交付可运行测试版验证数据流、空间比例、钻取和交互，再用基准数据收敛高性能指标。
- 测试版边界：
  - [x] 单栏当前目录、本地可读路径、逻辑字节数。
  - [ ] 首批结果可增量出现，扫描可取消，旧会话不会覆盖新会话。（2026-09-13 实测纠正：beta.1 前端等待 Done，增量显示未验收，由 BUG-0.1.5-001 修复。）
  - [x] Canvas 方块、白色细分隔线、选中描边、点击/双击钻取和面包屑返回。
  - [x] 基础音效沿用现有音效总线，并遵守静音和限流。
  - [ ] 典型目录规模、网络盘、双栏比较和最终 FPS/内存门槛进入后续基准评审。
- 初步验收方向：
  - [ ] 首层统计不读取文件内容，可取消，并以增量结果更新方块。
  - [ ] 方块面积与已统计字节数成比例，部分结果和未知项有明确表示。
  - [ ] 文件夹方块可钻取，面包屑、Esc/后退和现有导航语义保持一致。
  - [ ] 鼠标、键盘和拖拽选框产生一致的选择高亮与路径状态。
  - [ ] 音效事件可配置、可关闭、去重和节流。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-09-12 | `Accepted` | Codex | 用户确认 Platinum 设计稿，先按单栏本地目录测试版实现；大规模基准和双栏支持后续补充。 |

<a id="req-0-1-5-003"></a>

## `REQ-0.1.5-003` - 推进空间视图性能并审计 Everything 级搜索

### 评审输入

- 原始输入：[`01-original-input.md#req-0-1-5-003`](01-original-input.md#req-0-1-5-003)
- 评审日期：`2026-09-13`
- 当前结论：`Accepted`
- 优先级：`P1`

### 技术评审

- Space Sniffer 当前布局已改为 weighted strip treemap，布局和矩形生成是 `O(n)`；选中查找使用 `Set`，Canvas 像素缓冲只在尺寸或设备像素比变化时调整。
- 本节保存 `REQ-0.1.5-003` 实施时的历史审计基线。后续 `REQ-0.1.5-004` 已实现并实测 MFT 初建、USN 增量及权限分离 IPC，当前实现和测试版边界见 [后续评审](#req-0-1-5-004)。
- 审计时搜索分为五条链路：Browse 当前目录是已枚举会话的线性过滤；递归搜索每次查询重新 `fs::read_dir` DFS；全盘搜索使用最多 5 分钟的进程内索引，但首次建立和过期刷新仍是全盘 DFS，查询仍为 `O(N)` `contains`；Home 和 Compare 复用全盘/目录搜索；Duplicates 只过滤已完成的内容哈希分组，不是文件名搜索。
- 低风险优化已完成：目录条目缓存大小写折叠名称，避免 current、locate、sort 和 recursive 热路径反复分配字符串。该优化只降低常数，不改变索引模型。
- 与既定 Everything 级目标的差距在该次审计中确认：当时没有 NTFS MFT 初始枚举、USN Journal 增量监听、常驻索引服务、持久化索引或命名管道 IPC，因此当时不能宣称 Everything 级全盘瞬时搜索。

### 版本决策

- 是否延期：`No`
- 本版本接受范围：完成空间视图线性布局优化、搜索链路审计、性能分级和可回溯的服务化索引计划。
- 后续阶段原方案：Windows 索引服务负责 MFT 初建和 USN 增量，普通权限 GUI 通过受 ACL 保护的本地 IPC 查询；服务不可用时继续使用可取消遍历作为降级路径。该方案的测试版已在 `REQ-0.1.5-004` 以按需独立提权进程实现，未安装常驻 SCM 服务。

### 验收条件

- [x] Space Sniffer 布局、选中查找和 Canvas resize 路径完成线性化优化。
- [x] current、recursive、global、Home、Compare、Duplicates 搜索入口完成实现审计。
- [x] 审计明确记录当时的实现不满足 Everything 级 MFT+USN 目标。
- [x] 后续 `REQ-0.1.5-004` 已实现 MFT+USN 按需索引进程与权限分离 IPC，并完成真实磁盘样本验证；完整持久化和 Everything 全能力对等不属于此次测试版验收。

<a id="req-0-1-5-004"></a>

## `REQ-0.1.5-004` - 实现 MFT/USN 原生索引并交付测试版 EXE

- 结论：`Accepted`，优先级 P1，目标 V0.1.5，2026-09-13；依据用户明确推进实现的指示。
- 测试版验收状态：`Passed / 0.1.5-beta.1 delivered`；首次构建的两个空间发布阻断已在 `feat/0.1.5` 修复，全部门禁通过并按顺序提升 `release/0.1.5`，重新打包后的实际 EXE 原生索引与空间交互复验通过。构建来源、最终校验值及首次构建历史证据见 [`03-execution.md`](03-execution.md#req-0-1-5-004)。本结论针对下述测试版范围，不代表已完成 Everything 的完整能力或所有磁盘性能验收。
- NTFS 使用只读卷句柄、FSCTL_ENUM_USN_DATA 首次枚举 MFT 记录，FSCTL_QUERY_USN_JOURNAL / FSCTL_READ_USN_JOURNAL 维护变更；记录 file ID、parent ID 与名称，目录重命名必须影响后代路径。
- 捕获枚举前的 USN 水位并重放期间变更；journal ID 变化、水位丢失或不支持的记录版本触发重建或明确降级。不得静默发布不完整卷索引。
- 普通权限界面保留；同一 EXE 的独立索引进程通过 UAC 启动，使用限制本机及用户访问的命名管道。此测试版采用按需进程，不要求安装常驻 Windows SCM 服务。
- 界面必须显示索引引擎、构建/就绪/降级状态与原因，并提供启动原生索引操作。用户取消 UAC 或非 NTFS 路径采用可取消遍历降级。
- 文件名查询复用内存索引，禁止每次读取整个 JSON 或对全盘逐项 stat；尺寸与时间按命中结果补充，不能把 MFT 枚举的文件名记录当作完整空间统计。
- 空间视图保留逻辑字节扫描、钻取、选择和音效。测试包需标注原生文件名索引与空间字节统计的边界。
- 验收：解析异常/重命名/删除/日志断档/取消测试、查询与分页回归、真实 NTFS 原生枚举及变化验证（权限不足必须如实记录）、全套质量门禁、release/0.1.5 上测试版版本信息与 EXE/SHA256。

### 测试版边界与验证记录（2026-09-13）

- 原生 helper 在内存保存 MFT 记录和 USN 水位；每次重新启用会重建，运行期间只重放 USN。此前 JSON 为 portable 降级快照，不能标注为原生持久化。
- FSCTL_ENUM_USN_DATA 提供每个文件记录的主要名称；其他硬链接别名尚未完整覆盖。该边界在应用索引详情及测试包说明中明确展示。
- 查询缓存最多四份，共 200k 条/64 MB；按文件名匹配，不对所有记录 stat；显式按大小/时间排序或按时间过滤时补读匹配候选的元数据。
- 已完成真实 F 盘提权 helper 验证：20,387 条记录，构建含启动/授权耗时 2,047 ms，调试版查询含 IPC 16.675 ms；创建、文件改名、父目录改名、删除均经 USN 收敛。这是单机单盘样本，不能推广为所有磁盘的性能承诺。
- 首次构建 `0.1.5-beta.1` EXE 的 D 盘探针通过：169,001 条记录，初建含进程启动/授权 2,717 ms，首次查询含 IPC 15.9 ms；已有文件的 MFT 枚举和 USN 创建、文件改名、父目录改名、删除均验证通过。
- 首次构建 GUI 实机启用后，5 个磁盘、2,099,823 条记录显示就绪；截图 `native-index-ready.jpg` 随测试包保存。该条证明普通权限 GUI 与独立原生索引进程的完整启动链路，不能据此推断百万级查询耗时。
- 首次构建 GUI 从 C 盘浏览页面执行全盘搜索，可查到 D 盘测试包的 EXE；`native-global-search.jpg` 记录返回结果。空间视图实机打开 `D:\Muller\release`，206 MB、4 个文件夹完成渲染，单击目录的白色选框及属性显示正常。
- 首次构建质量门禁：147 个 Rust 测试、86 个前端测试、92 个 Edge E2E，以及 lint、生产构建、Rust fmt/clippy 通过。另 1 个要求提权的直接 provider 测试保持 ignored，由同 EXE 的真实提权 helper 探针补充验证；修复后的门禁结果见下文。
- 原生文件名索引仍不提供目录字节总量；空间视图继续单独扫描逻辑大小。原生索引重新启用或进程重启后需重建，不安装常驻 Windows SCM 服务；非 NTFS、日志不可用或授权取消时使用可取消的遍历降级。

### 首次构建的空间回归阻断（2026-09-13）

- 文件节点缺失：文件 `Work::Enter` 未安排 `Exit`，节点未进入 Batch；双击可进入仅含文件的目录并显示总字节数，但没有文件方块。`6dc173b` 已修复并增加文件批次回归；修复后 Rust 全量 148 个测试、fmt、clippy 通过。
- 中间层面包屑无动作：此前只处理索引 0。`908d552` 已按完整路径恢复实际历史目录，截断历史、清除选择并保留导航音效；扫描起点之前的祖先改为非按钮。两次钻取、中间层返回、Esc 回根的 Edge 定向回归通过。
- 两项修复均属于 `REQ-0.1.5-002` 的既有验收范围，已在 `feat/0.1.5` 实施并按顺序提升 `release/0.1.5`。修复后的 148 个 Rust、86 个前端、93 个 Edge 测试及 lint/build/fmt/clippy 全部通过；新 EXE 已完成以下复验，两个发布阻断均关闭。

### 修复后最终 EXE 验收（2026-09-13）

- 构建源 `c998709c5951809ebc1455fe6cef1b9e4d8ae6f8`，版本仍为 `0.1.5-beta.1`；最终 SHA256、文件大小及构建时间在执行文档中固化，与测试包 `manifest.json` 一致。
- 最终 D 盘原生探针 `native-probe-final-D.json`：168,919 条记录，初建 398 ms、查询含 IPC 10.713 ms，MFT 已有文件与 USN 创建、文件改名、父目录改名、删除全部通过。此次为本机重复运行，可能受 OS 缓存影响，不构成冷启动或所有磁盘的性能承诺。
- 最终 GUI 原生索引显示 5 盘、2,099,561 条记录就绪，证据 `final-native-index-ready.jpg`。
- 最终 GUI 在 release 浏览页切换全盘搜索，查询 `Muller-0.1.5-beta.1-x64.exe`，返回本次 13:31 构建的 EXE 与系统 prefetch 名称匹配项，证据 `final-native-global-search.jpg`。
- 空间视图从 release 方块双击进入 beta 目录后，11 MB、10 个文件块正常显示；点击 release 面包屑返回 206 MB、4 个目录；拖拽跨 beta/archive 选中 2 块，显示白色描边及 180 MB 详情。证据 `final-space-files.jpg`、`final-space-parent.jpg`。

<a id="bug-0-1-5-001"></a>

## `BUG-0.1.5-001` - 修正空间视图布局与真实渐进渲染

- 测试版交付更新（2026-09-13）：`0.1.5-beta.2 delivered`。最终 EXE 已运行并显示真实目录的汇总小项、灰黑方块和白色直角选择高亮；受控 Channel 的 Done 前画布变化回归通过。实机核验时用户正在连续操作窗口，停止争抢输入，最终 EXE 的扫描中连续截图尚未补齐，因此不把自动化流测试写成已完成的原生动画截图验收。
- 结论：`Accepted`，P1，目标 `0.1.5-beta.2`。这是用户已接受空间视图需求的缺陷修正。
- 根因：Rust 只在后序完成时发送节点，client 只在 Done 建树；布局以固定列数堆叠，缺少视口长宽比优化。此前关于渐进渲染完成的记录被本次实测纠正。
- 布局改为 squarified treemap：排序 O(n log n)、排布 O(n)，避免为了形式上的线性总复杂度牺牲矩形质量；按尺寸变化重排，不在动画每帧排序。面积保留真实字节比例，微小项可合并但不得人为夸大面积。
- 按已批准 SVG 恢复灰黑渐变、画布留白、细白层级线、正交选框、清晰且不拉伸的标签与检查器。对真实占比极小项提供汇总入口和列表。
- 用户追加的细框难点选与边框偏离反馈继续归入本条，结论 `Accepted`。独立方块需同时满足短边至少 `28 CSS px`、面积至少 `1600 CSS px²`；任一低于门槛的项按实际字节数合并到汇总项，并提供可点击的列表入口。汇总区域仍按实际字节占比绘制，不人为放大小项或汇总项面积；汇总区域过小时仍可通过列表入口访问成员。
- 边线验收以 [已批准 SVG](design/space-sniffer-wireframe.svg) 为准：灰黑背景上的细白光线、清晰的 90° 正交边界和直角选择高亮，避免粗重边框；普通、悬浮和选中状态须与该设计语言一致。
- 扫描 Batch 变为稳定路径 ID 的 upsert，及时发送发现的目录及增长中的祖先字节；scanning 与 partial 分别表达尚未完成、存在遗漏。不能在后序完成时重复累加。
- 前端合并批次并限频发布进度，首批即挂载地图；在后台继续统计，矩形按当前帧向真实目标平滑变化。无虚构百分比、无为演示而故意拖慢真实扫描。
- 路径切换、钻取和取消必须防止旧会话覆盖新视图，保留选择和正确导航历史；遵守系统减少动画设置。
- 验收：偏斜/大量节点的面积守恒、布局质量与性能；Done 前多次增长；取消/异常/重叠扫描；受控渐进流的 Edge 回归；实机截图对照既有设计；全套门禁后生成 beta.2 EXE。

### 当前验证记录（2026-09-13）

- 实现提交：`546f843`。全量 102 个前端测试、153 个 Rust 测试、96 个 Edge E2E 通过，lint、生产构建、Rust fmt/clippy 通过；质量门禁结果由主代理核对。
- 另 1 个要求提权的直接原生索引测试保持 ignored。本轮未修改原生索引，未重新执行此前 MFT/USN 实机探针；上版探针结果仅作为原版本历史证据。
- 5 项空间视图 Edge E2E 覆盖基础选择/钻取/返回、面包屑历史，以及受控 Channel 在 Done 前驱动画布变化、停止后的晚批次隔离、扫描中钻取与子目录进度/历史隔离、极小项可点击入口和减少动画设置。
- 当前状态：`beta.2 delivered; native in-progress capture pending`。最终 EXE 已运行，实际选择截图、构建来源及 SHA256 见执行文档；扫描中连续原生截图尚未补齐，自动化增量验证与静态窗口验收分别记录，尚未把本条整体标记 Done。

<a id="req-0-1-5-005"></a>

## `REQ-0.1.5-005` - 空间视图方向键、Target Cursor、黑透配色与右键操作

### 评审输入与结论

- 原始输入：[`01-original-input.md#req-0-1-5-005`](01-original-input.md#req-0-1-5-005)，包括后续右键菜单要求和 React Bits 源码附件。
- 评审日期：`2026-09-13`；结论：`Accepted`；优先级：`P2`；目标：`0.1.5-beta.3`。
- 用户已认可 beta.2 的现有视觉方向，本轮在此基础上增强交互与深色材质。用户认可不替代上一缺陷条目待补的原生扫描中连续截图。

### 技术评审与接受范围

1. 方向键按当前显示帧中可见矩形的空间位置选择邻居，不能按数组顺序模拟上下左右。首次按键建立可预测的初选，越界保持当前位置；扫描或插值更新后以稳定路径 ID 保留有效选择，并在下一次按键使用当前帧几何。复用 Enter 打开/钻取、Esc 返回和既有选择音效，音效继续遵守静音及限流。事件限定在空间视图交互区域，输入框、文本编辑、设置控件和菜单自身的方向键不被抢占。
2. 在设置的外观区域增加持久化 `cursorEffect = system | target`，默认 `system`；旧配置缺项或非法值回退系统光标，恢复默认设置也恢复系统光标。采用附件 React Bits Target Cursor 的旋转、四角锁定和点击反馈设计，适配项目 CSS 与 GSAP，不为单个视觉组件引入 Tailwind 构建链。
3. Target Cursor 支持普通 DOM 可操作控件，以及 Canvas 命中当前动画帧的虚拟矩形，不能只锁住整张画布。浮层使用 `pointer-events: none`；光标位置及锁定几何更新不通过高频 React state。停用、卸载、失焦、页面隐藏和触摸输入时停下动画并恢复可用系统光标；减少动画设置关闭旋转/视差或回退系统光标。清理事件、计时器和 GSAP 动画，避免残留隐形光标及后台持续绘制。
4. 本条明确更新 `REQ-0.1.5-002` 最初“只做灰阶”的视觉限制：Platinum 整体保留深黑，允许低饱和墨蓝、石墨、烟紫、青黑、暖黑等细微色差，通过稳定分配和局部反光增加层次。禁止大面积亮灰/纯白填充；保留细白分隔、直角高亮、可读标签、真实字节面积和小项列表入口，不因配色改变空间语义。
5. 右键按当前帧命中选择真实文件/目录，在既有多选内部右键保留多选；复用 Browse 的可用操作与语义，按能力提供打开/目录钻取、系统打开文件、定位浏览、复制路径、复制/剪切、重命名、新建目录、移入回收站和属性等适用项目。不可用的原生能力明确禁用；涉及确认、覆盖冲突或回收站的操作沿用已有行为，不新增永久删除或无确认批处理。成功改变目录内容后使相关空间快照失效并刷新，防止旧扫描晚到结果恢复已变更节点。聚合方块不是实际文件路径，须先从列表选中真实成员再执行文件操作。

### 风险与验证

- 几何导航风险：布局形状不规则、动画中的位置变化或节点聚合容易产生错选。用不规则布局、初选、边界、扫描更新和输入焦点隔离测试验证；按当前帧命中选择统一鼠标、键盘与光标的几何来源。
- 光标风险：应用级光标隐藏容易在停用、离开窗口或触摸切换时失去指示。覆盖配置迁移/恢复、启停/卸载、失焦/可见性、减少动画和输入设备切换，检查 RAF/GSAP 清理及点击穿透。
- 视觉验收：使用真实运行界面的同目录截图，检查深黑填充、色阶差异、边线与标签；Target Cursor 锁定控件与方块时均须可见。保留 beta.2 对照文件，不用新的静态截图替代扫描中动态证据。
- 文件操作风险：菜单选择对象与当帧方块必须一致；选中聚合项不向后端发送伪路径；成功修改后要取消旧扫描、重新统计。测试多选保留、单项命中、不可用项、关闭菜单恢复焦点以及操作后失效/刷新。
- 发布要求：lint、前端测试、生产构建、Rust fmt/test/clippy、Edge E2E 完整门禁后按 req → feat → release 推进，生成 beta.3 EXE、SHA256、说明与实际截图。保留 beta.2，`master` 不参与此次测试版交付。

### 验收状态

- [ ] 方向键空间邻居、初选/边界、Enter/Esc、焦点隔离和音效通过。
- [ ] Target Cursor 设置保存/恢复、DOM 与 Canvas 锁定、生命周期和减少动画通过。
- [ ] 黑透低亮配色与白色正交选框完成实际运行界面核验。
- [ ] 空间视图右键操作、真实对象选择、多选、聚合成员和刷新失效通过。
- [ ] 全部门禁通过，beta.3 EXE/哈希/构建来源和实机证据归档。

### 结论历史

| 日期 | 结论 | 评审人 | 说明 |
|---|---|---|---|
| 2026-09-13 | `Accepted` | Codex | 用户明确要求方向键、可选 Target Cursor、黑透色阶和右键文件操作；按既有 V0.1.5 测试版流程实施并交付 beta.3。 |

<a id="req-0-1-5-005"></a>

## `REQ-0.1.5-005` - 空间视图交互与材质增强

- 结论：`Accepted`，P1，目标 beta.3。
- 用户验收 beta.2 后追加方向键选择、可选 Target Cursor、右键菜单、深黑透亮配色和空间模式侧栏导航保持。
- 实现边界：右键菜单通过现有文件操作客户端执行；汇总块仅进入成员列表；方向键只在空间画布焦点内处理；Target Cursor 不劫持输入编辑/触控。
- 依赖：Target Cursor 使用附件 React Bits TypeScript 源码语义，采用 GSAP 与项目 CSS；需要保留归属说明。
- 风险与回滚：保留 beta.2 EXE 和构建清单，beta.3 可回退到 beta.2；不改变 MFT/USN 索引边界。

### 评审补充（2026-09-13，REQ-0.1.5-005）

- 接受单栏 Browse 常用操作映射：打开、打开方式、定位、复制/剪切/粘贴、复制名称/路径、终端、重命名、回收、新建、压缩/解压；比较和双栏入口排除。
- 接受 Smaller items 列表与空间选框统一选中语义、双击打开、右键菜单和键盘焦点；详情栏支持拖拽与方向键调宽。
- 接受清理指示与回收确认；操作成功后使空间快照重新扫描。
- Target Cursor 限定为空间方块虚拟命中区域，采用亮白四角边框，不覆盖应用其他控件。
